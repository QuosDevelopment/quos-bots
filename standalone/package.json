{
  "name": "quos-bots-standalone",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.mjs",
    "check": "node --check index.mjs && node --check research.mjs && node --check terry.mjs && node --check state.mjs",
    "test": "node smoke.mjs",
    "bootstrap": "node index.mjs --bootstrap-only",
    "migrate:file-to-postgres": "node migrate-file-to-postgres.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "discord.js": "^14.27.0",
    "firebase-admin": "^13.4.0",
    "node-cron": "^4.2.1",
    "pg": "^8.16.3"
  }
}
